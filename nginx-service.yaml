apiVersion: v1
kind: Service
metadata:
  name: rger-nginx-service
  namespace: rger-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80